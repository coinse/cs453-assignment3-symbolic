## do this for the bonus goal

def advanced(x, y):
    x = x + y
    y = y - 12

    if x == y:
        if y == 42:
            print("y is 30!")
        else:
            y = y + 3
            z = 3 * y - x
            if z < x:
                print("z is less than x")
            else:
                print("z is not less than x")